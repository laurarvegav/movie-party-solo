<h1>Viewing Party</h1>

<%= form_with( url: user_movies_path(@user.id), local: true, method: :get) do |f| %>
  <%= f.button "Discover Top Rated Movies" %>
<% end %>


<%= form_with(url: user_movies_path(@user.id), local: true, method: :get) do |f| %>
  <%= f.text_field :movie_title %>
  <%= f.button "Find Movies" %>
<% end %>